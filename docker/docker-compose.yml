version: '3.8'

services:
  trans-db:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5432:5432"
    volumes:
      - db-data:/data
    command: ["--port", "5432", "--shards", "3", "--data-dir", "/data"]

volumes:
  db-data:
